export enum BookingStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}


export enum BookingTimeFilter {
  UPCOMING = 'upcoming',
  PAST = 'past',
}

export enum UserType {
  PROVIDER = 'provider',
  ADMIN = 'admin',
}